Users
  * authentication 
  * signup

NewsFeed
  * parsing project out of item xml if present
  * extract content + title into seperate field, free of presentation markup
  * create new feed in test so fixtures aren't messing with our NewsItem count?
  * how to mock update http calls to GitHub?

